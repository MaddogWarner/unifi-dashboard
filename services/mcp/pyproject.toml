[project]
name = "unifi-dashboard-mcp"
version = "0.0.0" # placeholder — release version comes from the git tag (see docker-publish.yml)
requires-python = ">=3.12"
dependencies = [
  "mcp[cli]>=1.0",
  "httpx>=0.27",
  "pydantic>=2.0",
]
